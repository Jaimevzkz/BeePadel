CREATE TABLE matchEntity (
  matchId TEXT NOT NULL PRIMARY KEY,
  dateTimeUtc TEXT NOT NULL
);

getAllMatches:
SELECT *
FROM matchEntity;

insertOrReplaceMatch:
INSERT OR REPLACE INTO matchEntity(matchId, dateTimeUtc)
VALUES (?, ?);

deleteMatchById:
DELETE FROM matchEntity
WHERE  matchId = :id;
